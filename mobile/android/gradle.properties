org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Desactiva la compilación incremental de Kotlin. Necesario porque el proyecto
# (D:) y la Pub Cache (C:) están en discos distintos: el compilador incremental
# falla al calcular rutas relativas entre raíces de disco diferentes en Windows
# ("this and base files have different roots").
kotlin.incremental=false
